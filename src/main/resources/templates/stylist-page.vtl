<h1>Available Stylists!</h1>
#if(!$stylists)
  <h4>There Are Currently No Availble Stylists</h4>
#else
  <form action="/finalist" method="post">
    #foreach($squad.getName() in $squadList)
      <p class="squads">
        <label for=$squad.getName()>
        <input type="checkbox" name="squadSelect" value=$squad.getId()>
          $squad.getName()
        </label>
      </p>
    #end
    <button type="submit" class="btn btn-default">Join Squad!</button>
  </form>
#end
<a href="/squad-registration-form">CREATE YOUR OWN SQUAD</a>
